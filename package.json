{
  "name": "whatsapp-social-bridge",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "dotenv -e .env -- npm run dev --workspaces",
    "build": "dotenv -e .env -- npm run build --workspaces",
    "start": "dotenv -e .env -- npm run start --workspaces"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
